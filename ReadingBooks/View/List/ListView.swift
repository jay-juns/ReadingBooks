import SwiftUI

struct ListView: View {
    var body: some View {
        VStack {
            Text("ListView 페이지입니다.")
            // ListView의 추가적인 구현 내용
        }
        .navigationBarTitle("ListView", displayMode: .inline)
    }
}
